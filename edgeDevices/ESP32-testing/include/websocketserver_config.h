char path[] = "/";   //identifier of this device
// in the future, discover the IP address corresponding the the did:key
//char host[] = "192.168.1.155"; //replace this ip address with the ip address of your Node.Js server /// hostname -I (in linux)
char host[] = "10.0.0.4"; //replace this ip address with the ip address of your Node.Js server /// hostname -I (in linux)
const int espport= 3000;

/*
unsigned long previousMillis = 0;
unsigned long currentMillis;
unsigned long interval=300; //interval for sending data to the websocket server in ms
*/

